# Sender Configuration

Create and manage Vector Agent TOML and Fluent Bit configuration files.

Select or add equipment, enter metadata and central connection details, configure include/exclude paths, file detection, multiline, resend behavior, and transport buffer, then validate, save, and download.

Fluent Bit configurations expose tail input, multiline, storage, and forward options separately. Download a needed configuration before deleting it from the server.
