# Operations Diagnostics

This mission-control view combines Vector, Oracle, equipment, and throughput health.

Check the 10-minute buffer/memory/insert trends, disk-buffer composition, source and sink event counts, port 6000 connections, Oracle pool, ingestion rate, lag, and equipment matrix.

When troubleshooting, verify service connectivity first, then compare source input, sink output, buffer growth, and Oracle throughput.
