# VRL Simulator

Run sample input and VRL through the Backend simulator. The primary **VRL & Mapping** screen combines AI generation, editing, simulation, TOML apply, and target mapping. Validate normal, empty, and malformed samples before applying.
