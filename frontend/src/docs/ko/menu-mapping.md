# 타겟 매핑

VRL `data.*` 필드를 Oracle TABLE 컬럼 또는 PROCEDURE 파라미터에 연결하는 보조 화면입니다.

현재 기본 작업 화면은 **VRL & 매핑**입니다. 그 화면에서 설비를 선택하고 TABLE/PROCEDURE 타겟, 컬럼 연결과 저장을 한 흐름으로 처리하세요. 실제 적재 매핑의 단일 소스는 `config/table-registry.json`입니다.
